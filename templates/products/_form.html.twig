<a href="{{ path('app_products_index') }}"><button class="btn btn-primary">Retour à la liste des produits</button></a>
{{ form_start(form) }}
{{ form_widget(form) }}
<br>
<button class="btn btn-success">{{action}}</button>
<input type="reset" class="btn btn-danger"/>
{{ form_end(form) }}