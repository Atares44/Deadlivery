{{ form_start(form) }}
{{ form_widget(form) }}
<br>
<button class="btn btn-success">{{action}}</button>
<input type="reset" class="btn btn-danger"/>
{{ form_end(form) }}
<a href=""><button class="btn btn-primary">Retour à l'accueil</button></a>
