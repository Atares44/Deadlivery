{{ form_start(form) }}
<p id="error"></p>
{{ form_widget(form) }}
<br>
<table id="recap" class="table table-dark"></table>
<table id="recapDates" class="table table-dark"></table>
{% if dateNow is defined %}
    <input name="dateNow" type="hidden" value="{{ dateNow|date('Y-m-d H:i:s') }}">
{% endif %}
<button id="confirm" class="btn btn-success">{{action}}</button>
<input type="reset" class="btn btn-danger"/>
{{ form_end(form) }}
